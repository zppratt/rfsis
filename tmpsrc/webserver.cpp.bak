#include "webserver.hpp"

int main(void) {
    webServer ws(NULL, 0, 0, 0, 0, 8080, "192.168.1.101", "255.255.255.0" );
    pico_stack_init();

    ws.setDev(ws.init_picotcp());
    /*if(ws.getDev() == NULL) {
        printf("Could not make device!\n");
        return -1;
    }*/

    ws.setup_server();

    pico_stack_loop();

    return 0;
}
